import numpy as np
from scipy import special, optimize
import matplotlib.pyplot as plt

print('Hello World')

#Just added a matrix and printet it, arrange creates 15 long vector, and then reshapes it into a matrix
a = np.arange(15).reshape(3,5)
print(a)

#created a vector with evenly spaced numbers from -100 to 100 spaced with 1
x = np.linspace(-100,1,100)
print(x)

